services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:/data/local.db
      ENCRYPTION_KEY_PATH: /data/.keyfile
    volumes:
      - app-data:/data

volumes:
  app-data:
