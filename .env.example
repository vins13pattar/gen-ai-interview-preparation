DATABASE_URL="file:./local.db"
ENCRYPTION_KEY_PATH=".keyfile"

# Optional: require a browser session (httpOnly cookie) before any page or API (except /login and auth routes).
# Use a long random value. For faster verification (skip PBKDF2 on each request), set APP_SESSION_SECRET too.
# APP_ACCESS_PASSWORD=""
# APP_SESSION_SECRET=""
